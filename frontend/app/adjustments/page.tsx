import AppShell from "@/components/AppShell";import Header from "@/components/Header";
const notes=[["Evidence","Data window, sample size, benchmark, and supporting metric."],["Change","Scenario version plus current and proposed parameter values."],["Risk","Expected benefit, downside, affected limits, and failure conditions."],["Validation","Research test, observation window, and reversible rollback plan."],["Decision","Proposed, reviewed, accepted for research, or rejected."]];
export default function AdjustmentsPage(){return <AppShell><Header title="Suggested Adjustments" subtitle="Reviewable proposals with evidence, risk, and decision notes."/><div className="guardrail">Suggestions are proposals only. They are never applied automatically and cannot bypass strategy, RiskEngine, or execution controls.</div><div className="grid"><section className="card span8"><div className="card-title">Suggestion notes</div><div className="readiness-list">{notes.map(([name,detail])=><div className="readiness-row" key={name}><strong>{name}</strong><span>{detail}</span></div>)}</div></section><section className="card span4"><div className="card-title">Current queue</div><p className="sub">No suggestions yet. Aegis will not invent recommendations before validated market data and research results exist.</p><p className="bad">NO AUTOMATIC EXECUTION</p></section></div></AppShell>}
