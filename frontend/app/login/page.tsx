"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {api} from "@/lib/api";
export default function Login(){const router=useRouter();const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const form=new FormData(e.currentTarget);try{await api('/api/auth/login',{method:'POST',body:JSON.stringify({username:form.get('username'),password:form.get('password')})});router.push('/')}catch(e){setError(e instanceof Error?e.message:'Login failed')}finally{setBusy(false)}}return <main className="login-page"><form className="login-card" onSubmit={submit}><div className="mark">A</div><div className="eyebrow" style={{marginTop:22}}>Secure operator access</div><h1>AEGIS ALPHA</h1><p className="sub">Authenticate through the Ubuntu PAM security boundary. Your password is never stored by Aegis.</p><div className="field"><label>Authorized user</label><input name="username" defaultValue="nathan" autoComplete="username" required/></div><div className="field"><label>Linux account password</label><input name="password" type="password" autoComplete="current-password" required/></div>{error&&<p className="error">{error}</p>}<button className="primary" disabled={busy}>{busy?'AUTHENTICATING…':'ENTER CONSOLE'}</button><p className="sub" style={{fontSize:10,marginTop:20}}>Trading execution is disabled · Sessions expire automatically</p></form></main>}

