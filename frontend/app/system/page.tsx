"use client";import{useEffect,useState}from"react";import AppShell from"@/components/AppShell";import Header from"@/components/Header";import{api}from"@/lib/api";
type System={application:string;backend_version:string;environment:string;postgresql:string;redis:string;uptime_seconds:number;server_time:string;trading:string};export default function SystemPage(){const[s,setS]=useState<System|null>(null);useEffect(()=>{api<System>('/api/system').then(setS)},[]);return <AppShell><Header title="System Telemetry" subtitle="Runtime identity, connectivity, and uptime from the protected backend."/><section className="card">{!s?<div className="loading">Loading system telemetry…</div>:Object.entries(s).map(([k,v])=><div className="status-row" key={k}><span>{k.replaceAll('_',' ')}</span><strong className={v==='CONNECTED'?'healthy':v==='DISABLED'?'bad':''}>{String(v)}</strong></div>)}</section></AppShell>}

