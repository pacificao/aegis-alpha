import AppShell from"@/components/AppShell";import Header from"@/components/Header";
export default function Security(){return <AppShell><Header title="Security Boundary" subtitle="Controls are architectural constraints, not optional application features."/><div className="grid"><section className="card span8"><div className="card-title">Enforced architecture</div><ol className="principles"><li>Only the deployment-configured operator is authorized through host PAM</li><li>Linux credentials are never stored by Aegis</li><li>AI proposes; deterministic systems decide and authorize</li><li>RiskEngine cannot be bypassed</li><li>Research and execution use separate security domains</li><li>Production secrets remain unavailable to development agents</li><li>PostgreSQL, Redis, FastAPI are private-network only</li><li>All mutations produce development activity records</li></ol></section><section className="card span4"><div className="card-title">Execution boundary</div><strong className="bad" style={{display:'block',fontSize:24,margin:'20px 0'}}>TRADING DISABLED</strong><p className="sub">Robinhood is connected through the isolated read-only gateway. Authorization material stays outside Aegis; all order, review, cancellation, and mutation tools are blocked.</p></section><section className="card span12"><div className="card-title">Host firewall</div><p className="sub">UFW is active with default-deny inbound policy. SSH, HTTP, and HTTPS are allowed; PostgreSQL, Redis, FastAPI, Next.js, and the broker gateway have no public host listeners.</p></section></div></AppShell>}
