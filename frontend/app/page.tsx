"use client";import {useState}from"react";import AppShell from"@/components/AppShell";import Header from"@/components/Header";
const periods=["1D","1W","1M","3M","YTD","1Y","ALL"],metrics=[["Portfolio value","Net liquidation value"],["Total return","Selected period · vs benchmark"],["Available capital","Cash not committed"],["Drawdown","Current · maximum"]],panels=[["Portfolio performance","Equity curve, benchmark, contributions, deposits, and withdrawals."],["Risk & exposure","Allocation, sector concentration, factor exposure, drawdown, and risk-budget use."],["Scenario contribution","Realized and unrealized contribution by scenario with confidence and sample size."],["Opportunity intelligence","Ranked candidates, expected edge, capital required, constraints, and evidence freshness."]];
function EmptyChart({title,detail}:{title:string;detail:string}){return <section className="card decision-panel"><div className="panel-heading"><div><div className="card-title">{title}</div><p className="sub">{detail}</p></div><span className="data-badge">AWAITING DATA</span></div><div className="chart-empty"><div className="chart-grid"/><span>Validated market and portfolio data will render here.</span></div></section>}
export default function Dashboard(){const[period,setPeriod]=useState("1M");return <AppShell><Header title="Decision Intelligence" subtitle="Portfolio, risk, performance, and opportunity evidence for high-impact decisions."/><section className="decision-filters" aria-label="Dashboard filters"><div className="period-filter" aria-label="Time period">{periods.map(value=><button key={value} className={period===value?"selected":""} onClick={()=>setPeriod(value)}>{value}</button>)}</div><label>Account<select disabled><option>All accounts</option></select></label><label>Scenario<select disabled><option>All scenarios</option></select></label><label>Benchmark<select disabled><option>S&amp;P 500</option></select></label></section><div className="dashboard-notice"><span>RESEARCH VIEW</span> Metrics remain blank until validated data is available. No values are estimated or fabricated.</div><div className="grid">{metrics.map(([label,detail])=><div className="card metric" key={label}><label>{label}</label><strong>—</strong><span className="sub">{detail}</span></div>)}<div className="decision-grid span12">{panels.map(([title,detail])=><EmptyChart key={title} title={title} detail={detail}/>)}</div><section className="card span8"><div className="panel-heading"><div><div className="card-title">Decision queue</div><p className="sub">Proposals requiring operator review, ordered by urgency and impact.</p></div><span className="data-badge">0 ITEMS</span></div><div className="decision-empty">No evidence-backed decisions are ready.</div></section><section className="card span4"><div className="card-title">Guardrails</div><div className="status-row"><span>Trading</span><strong className="bad">DISABLED</strong></div><div className="status-row"><span>Automatic changes</span><strong className="bad">PROHIBITED</strong></div><div className="status-row"><span>Data freshness</span><strong className="warn">NO DATA</strong></div></section></div></AppShell>}

